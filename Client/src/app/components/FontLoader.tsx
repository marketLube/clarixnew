"use client";

/**
 * FontLoader is no longer needed. Poppins is now loaded via next/font/google
 * in the root layout for optimal performance (self-hosted, no layout shift).
 *
 * This file is kept as a no-op to avoid breaking any residual imports.
 */
export default function FontLoader() {
  return null;
}
